<?xml version="1.0" encoding="UTF-8"?>
<ListView xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	x:Class="SportChallengeMatchRank.Shared.LeagueListView"
	IsRefreshing="{Binding IsBusy, Mode=OneWay}"
	xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions"
	xmlns:local="clr-namespace:SportChallengeMatchRank.Shared;assembly=SportChallengeMatchRank" RowHeight="60">
	<ListView.ItemTemplate>
		<DataTemplate>
			<ViewCell>
				<ViewCell.View>
					<Grid>
						<StackLayout Padding="30,10,10,10" Orientation="Horizontal" Spacing="20"
							IsVisible="{Binding Id, Converter={x:Static local:IsNotNullToBoolConverter.Instance}}">

							<controls:CircleImage x:Name="photoImage" Source="{Binding ImageUrl}"
								HorizontalOptions="Start" VerticalOptions="Center" Aspect="AspectFill" WidthRequest="40" HeightRequest="40"  />

							<StackLayout Spacing="-1">
								<Label Text="{Binding Name}" FontSize="20" VerticalOptions="Center" />
								<Label Text="{Binding Sport, StringFormat='the noble pastime of \{0\}'}" FontSize="10" VerticalOptions="Center" />
							</StackLayout>
						</StackLayout>
					</Grid>
				</ViewCell.View>
			</ViewCell>
		</DataTemplate>
	</ListView.ItemTemplate>
</ListView>