<?xml version="1.0" encoding="UTF-8"?>
<local:LeagueDetailsXaml xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:local="clr-namespace:SportChallengeMatchRank.Shared;assembly=SportChallengeMatchRank"
	BackgroundColor="{StaticResource grayLight}"
	x:Class="SportChallengeMatchRank.Shared.LeagueDetailsPage">
	<local:LeagueDetailsXaml.Content>
	<ScrollView>
		<Grid VerticalOptions="Fill">
			<Grid.RowDefinitions>
				<RowDefinition Height="*" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<StackLayout Orientation="Vertical" Spacing="20" VerticalOptions="Fill">
				<StackLayout Padding="24,24,24,0" Spacing="4">
					<Label FontSize="Small" Text="{Binding League.Sport, StringFormat='the honorable pastime of \{0\}'}" TextColor="{StaticResource grayPrimary}" />
					<Label Text="{Binding League.Name}" FontSize="Large" />
				</StackLayout>

				<Image x:Name="photoImage" Source="{Binding League.ImageUrl}" HorizontalOptions="Fill" HeightRequest="200"
					VerticalOptions="Center" Aspect="AspectFill" />

				<StackLayout Padding="24,0,24,14" Spacing="4">
					<Label FontSize="Small" Text="This league was created on Monday, August 12, 2015 and has 15 members. Joseph Sylvester, AKA 'The Hound' is currently holding the number one spot."
						 VerticalOptions="Center" />
				</StackLayout>

				<StackLayout Padding="24,0,24,14" Spacing="8" IsVisible="{Binding IsMember}">
					<Label FontSize="Medium" Text="YOUR STATS" TextColor="{StaticResource grayDark}" />
					<BoxView BackgroundColor="{StaticResource grayPrimary}" HeightRequest=".5" HorizontalOptions="FillAndExpand" />
					<StackLayout Orientation="Horizontal">
						<Label FontSize="Small" Text="Rank" TextColor="{StaticResource grayDark}" />
						<Label FontSize="Small" Text="{Binding MembershipViewModel.RankDescription}" TextColor="{StaticResource grayDark}" HorizontalOptions="EndAndExpand" />
					</StackLayout>
					<BoxView BackgroundColor="{StaticResource grayPrimary}" HeightRequest=".5" HorizontalOptions="FillAndExpand" />
					<StackLayout Orientation="Horizontal">
						<Label FontSize="Small" Text="Stats" TextColor="{StaticResource grayDark}" />
						<Label FontSize="Small" Text="{Binding MembershipViewModel.Stats}" TextColor="{StaticResource grayDark}" HorizontalOptions="EndAndExpand" />
					</StackLayout>
					<BoxView BackgroundColor="{StaticResource grayPrimary}" HeightRequest=".5" HorizontalOptions="FillAndExpand" />
					
					<StackLayout Padding="0,40,0,20" IsVisible="{Binding OngoingChallenge, Converter={x:Static local:IsNotNullToBoolConverter.Instance}}">
						<local:ChallengeCardView x:Name="cardView" ViewModel="{Binding ChallengeViewModel}" />
					</StackLayout>
				</StackLayout>
			</StackLayout>

			<StackLayout Grid.Row="1" VerticalOptions="EndAndExpand" Padding="28" Spacing="14">
				<Button x:Name="btnRankings" Text="LEADERBOARD" HorizontalOptions="FillAndExpand" Clicked="OnRankingsClicked"
					IsEnabled="{Binding IsBusy, Converter={x:Static local:InverseBoolConverter.Instance}}"
					Style="{StaticResource grayButtonStyle}" />

				<Button x:Name="btnJoin" Text="JOIN LEAGUE" HorizontalOptions="FillAndExpand"
					Style="{StaticResource blueButtonStyle}"
					IsVisible="{Binding IsMember, Converter={x:Static local:InverseBoolConverter.Instance}}"
					IsEnabled="{Binding IsBusy, Converter={x:Static local:InverseBoolConverter.Instance}}" />
			</StackLayout>

		</Grid>
	</ScrollView>

	</local:LeagueDetailsXaml.Content>
</local:LeagueDetailsXaml>